# Skills core/packages

Skills transversales para integrar paquetes internos `@platform/*` en apps y APIs.

## Skills disponibles

- `skills/packages-integration-bridge/SKILL.md`: worker para `new:package` y wiring en `new:template`/`new:mvp`.
